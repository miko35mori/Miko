<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>動物愛護団体 - カレンダーページ</title>
  <style>
    body {
      font-family: 'Limelight', sans-serif;
      background-color: #EEF9FF;
      color: #665635;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    header {
      background-color: #C6E6F0;
      padding: 10px 0;
      text-align: center;
      width: 100%;
      height: 100px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 20px;
    }

    

    h3 {
      text-align: center;
      font-weight: normal;
      margin-bottom: 10px;
      margin-left: 50px;
    }

    h1 {
      padding: 10px;
      margin-top: 10px;
      font-size: 1.65em;
      font-weight: bold;
      text-align: center;
      color: #665635;
      margin-left: 10px;
    }

    .title-container h1 {
      text-align: center;
    }


    .title-container {
    font-size: 1em;
    font-weight: bold;
    margin: 10px auto 0;
    align-items: center;
    width: 500px;
    z-index: 1000; /* 適切な値に設定してください */
}
    
    .calendar-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
      margin-top: 20px;
      max-width: 1200px; /* 最大幅を設定 */
      margin: 20px auto;
      z-index: 999;
    }

    .day {
      border: none;
      text-align: center;
      height: 60px; /* セルの高さを適切に指定 */
      width: 60px;
      padding: 30px; /* より大きな日付のパディング */
      margin: 5px; /* 日付の周りの余白を追加 */
      background-color: #fff
      border: 2px solid #ddd; /* 枠線の太さを増やす */
      border-radius: 8px; /* 枠線の角を丸くする */
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* 枠線に影を追加 */
      transition: background-color 0.3s ease; /* ホバーエフェクト *
   }

    .day span {
  position: absolute;
  top: 5px;
  left: 5px;
  width: 100%;
  text-align: left; /* 追加 */
  font-size: 14px; /* 適切なサイズに調整 */
  font-weight: bold; /* 適切な太さに調整 */
  color: #665635; /* 適切な色に調整 */
}
    
    .day:hover {
      background-color: #f0f0f0; /* ホバー時の背景色 */
    }
  

    .today {
      background-color: #C6E6F0;
      color: #665635;
      font-weight: bold;
    }

    

    .add-event {
  display: inline-block;
  width: 30%;
  padding: 10px;
  margin-bottom: 10px;
  background-color: #F2D7D9;
  color: #665635;
  text-align: center;
  text-decoration: none;
  border-radius: 5px;
  margin-left: 170px;
  margin-top: 0px;
  margin-bottom: 20px;
}

    .add-event:hover {
      background-color: #F2D1D1;
    }

    
  </style>
</head>
<body>
<br>
<br>
<div class="title-container">
  <h1>✦　動物イベント カレンダー　✦</h1>
   <h4> <%= link_to "新規投稿へ", new_blog_path, class: 'add-event' %></h4>
  </div>

  <div class="calendar-container">
    <%= month_calendar events: @blogs do |date, blogs| %>
      <div class="day <%= 'today' if date.today? %>">
        <span><%= date.day %></span>
        <br>
        <% blogs.each do |blog| %>
          <div class="event" style="background-color: <%= ['#DDEEAA', '#DDDDFF', '#FCECEA', '#FFFFDD'].sample %>;">
            <%= link_to blog.title, blog %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

</body>
</html>


