<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>新規投稿 - 動物愛護団体</title>
  <style>
    body {
      font-family: 'Limelight', sans-serif;
      background-color: #EEF9FF;
      color: #665635;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    header {
      background-color: #C6E6F0;
      padding: 10px 0;
      text-align: center;
      width: 100%;
      height: 100px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 20px;
    }


    .post-container {
      background-color: #FCECEA;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 110%;
      max-width: 1200px;
      margin-top: 20px;
      z-index: 0;
    }

    h3 {
      text-align: center;
      font-weight: bold;
      margin-bottom: 0px;
    }

    .post-container h1 {
      text-align: center;
      font-size: 1.8em;
      color: #665635;
      margin: 0;
      display: block;
      width: 100%;
    }

    form {
      margin-top: 20px;
    }

    .container {
      display: flex;
      align-items: baseline;
      color: #665635;
      margin-bottom: 10px;
    }

    .container > span {
      width: 100%;
      margin-left: 10px;
      font_color: #665635
      padding-right: 10px;
      text-align: flex;
      display: inline-block; /* インラインブロックに変更 */
    }

    .blog-link {
      text-decoration: none;
      color: #665635; /* 適切な色に調整 */
      margin-left: 200px;
    }
  
    .blog-link:hover {
      color: #B89C9C /* ホバー時の色 */
    }


    input[type="text"],
    select,
    textarea,
    input[type="file"],
    input[type="submit"] {
      width: 110%;
      padding: 10px;
      margin-top: 8px;
      box-sizing: border-box;
      border: 1px solid #cccccc;
      border-radius: 4px;
      height:
    }

    input[type="submit"] {
      background-color:  #DBB0B0;
      width: 70%;
      padding: 10px;
      margin-top: 8px;
      box-sizing: border-box;
      border: 1px solid #cccccc;
      border-radius: 4px;
      margin-left: 100px;
    }

    input[type="submit"]:hover {
      background-color: #F2D7D9;
    }

    .container .wide-text-field {
      width: 100%;
      padding: 10px;
      margin-top: 8px;
      box-sizing: border-box;
      border: 1px solid #cccccc;
      border-radius: 4px;
      height: 100px; /* 適切な高さに調整 */
    }


    a {
      text-align: center;
      color: #665635;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <br>
  <br>
  <div class="post-container">
    <h1>イベント情報</h1>
    <h3>新規投稿</h3>

    <%= form_with(model: @blog, local: true) do |form| %>
      <div class="container">
        <span>◀ タイトル</span> <%= form.text_field :title %>
      </div>

      <div class="container">
        <span>◀ 日時</span> <%= form.datetime_select :start_time %>
      </div>

      <div class="container">
        <span>◀ 内容</span> <%= form.text_field :content, class: 'wide-text-field' %>
      </div>

      <div class="submit">
        <%= form.submit "イベントを追加する" %>
      </div>
    <% end %>
   <br>
  
    <a><%= link_to "カレンダーに戻る", blogs_path,  class: 'blog-link' %></a>
  </div>
</body>
</html>
